import { Box, Typography } from "@mui/material";

const DashboardContent = () => {
  return (
    <Box sx={{ padding: 4, textAlign: "center" }}>
      <Typography variant="h4">Bienvenido al Dashboard Pro</Typography>
      <Typography variant="body1">Aquí irá el contenido principal.</Typography>
    </Box>  
  );
};

export default DashboardContent;
